[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "android-workbench"
version = "0.1.0"
description = "Shared local scheduling for Android analysis sessions"
requires-python = ">=3.11"
dependencies = []
[project.scripts]
android-workbench = "workbench.cli:main"
[tool.setuptools]
packages = ["workbench"]
